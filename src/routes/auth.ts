import { Router } from 'express';

const router = Router();

// POST
router.post('/signup', () => {});
router.post('/login', () => {});
router.post('/logout', () => {});
router.post('/google', () => {});

export default router;
